COMPOSE_PROJECT_NAME=ez-local-hermetic-functional-tests
ENFORCER_IMAGE=${ENFORCER_IMAGE}
SUT_NAME=${SUT_NAME}
HOSTNAME=${HOSTNAME}
PORT=${PORT}
MANIFEST_NAME=${MANIFEST_NAME}
BUNDLE_NAME=${BUNDLE_NAME}
BUNDLE_DIR=${BUNDLE_DIR}
EXTRA_ENFORCER_ARGS=${EXTRA_ENFORCER_ARGS}
